{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Resources list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Controllername</th>
                <th>Actionname</th>
                <th>Routename</th>
                <th>Status</th>
                <th>Createdat</th>
                <th>Updatedat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('resources_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.controllerName }}</td>
                <td>{{ entity.actionName }}</td>
                <td>{{ entity.routeName }}</td>
                <td>{{ entity.status }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('resources_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('resources_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('resources_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
