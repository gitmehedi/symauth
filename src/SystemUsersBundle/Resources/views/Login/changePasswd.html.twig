{% extends "::layout.html.twig"%}
{% block body -%}
    {# <div class="col-md-6 col-md-offset-3">
         <div class="login-panel panel panel-default">
             <div class="panel-heading">
                 <h3 class="panel-title">Password Change</h3>
             </div>
             <div class="panel-body">
                 <form action="{{ path('change_passowrd') }}" method="post"  role="form">
                     <fieldset>
                         <div class="form-group">
                             <input type="password" class="form-control" autofocus="" id="oldpassword" name="oldPassword" placeholder="Provide Old Password">
                         </div>
                         <div class="form-group">
                             <input type="password" value="" name="_password" placeholder="Password" minlength="8" class="form-control">
                         </div>
                         <button type="submit" class="btn btn-lg btn-success btn-block">Change Password</button>
                     </fieldset>
                 </form>
             </div>
         </div>
     </div>#}
    {{ form_start(form)}}
    <div class="row">
        {{ form_row(form.plainpassword)}}
        {{ form_row(form.resetPassword)}}
        
        {{ include('SystemUsersBundle:Form:button.html.twig', {'form': form}) }}
    </div>

    {{ form_end(form)}}

{% endblock %}